import java.util.ArrayList;

public class SpecialMachine extends RegularMachine {
    public SpecialMachine(ArrayList<Items> items, int[][] machineBalance)
    {
        super(items, machineBalance);
    }
}
